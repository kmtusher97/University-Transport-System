import { } from "../actions/types";

const initailState = {
  routes: [],
  route: {}
};

export default function (state = initailState, action) {
  switch (action.type) {


    default:
      return state;
  }
}