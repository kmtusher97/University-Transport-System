import { } from "../actions/ScheduleActions";

const initialSate = {
  schedules: [],
  schedule: {}
};

export default function (state = initialSate, action) {
  switch (action.type) {

    default:
      return state;
  }
}