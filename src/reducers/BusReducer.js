import { } from "../actions/BusActions";

const initialState = {
  buses: [],
  bus: {}
};

export default function (state = initialState, action) {
  switch (action.type) {

    default:
      return state;
  }
}